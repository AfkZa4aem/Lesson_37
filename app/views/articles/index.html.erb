<% @articles.each do |art| %>
  <h2>
    <%= art.title %>
  </h2>
  <p>
    <%= art.text %>
  </p>
  <p>
    <%= link_to "Show", article_path(art) %> | 
    <%= link_to "Edit", edit_article_path(art) %>
  </p>
  <hr>
<% end %>